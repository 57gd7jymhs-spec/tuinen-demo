# Tuinen Decoster website

**Project source of truth: [`AGENTS.md`](./AGENTS.md). Read it first.**
It holds the status, stack, brand tokens, contact data (real vs invented), the before-publish
checklist, key files, and the dated work log — shared with ChatGPT Codex. Do not duplicate those
facts here; update `AGENTS.md` instead, and append to its work log at the end of each session.

## Inheritance
Global rules in `C:\Users\Apollo\.claude\CLAUDE.md` apply (two-pass screenshot workflow, design
principles, parallel execution). This file and `AGENTS.md` override only where they conflict.
